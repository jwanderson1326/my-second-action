name: TestingTesting

on:
  issue_comment:
    types: [created]
  pull_request:
    types: [opened]
    branches:
      - main
  push:
    branches:
      - "*"

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest

    steps:
    - name: Testing
      run: |
        echo $GITHUB_EVENT_NAME
        echo $GITHUB_EVENT_PATH
        cat $GITHUB_EVENT_PATH


